# ============================================================
# requirements.txt
# Python 3.10+  |  CUDA 12.1 recommended (also tested on 11.8)
# ============================================================
#
# CUDA-enabled PyTorch — install BEFORE the rest of this file:
#
#   CUDA 12.1:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
#   CUDA 11.8:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
#   CPU-only (development / no GPU):
#   pip install torch torchvision
#
# Then install everything else:
#   pip install -r requirements.txt
# ============================================================

# Core ML
# transformers version is a tight constraint — see note below.
#   - LLaDA-8B-Instruct remote code needs the pre-5.0 tied-weights API
#     (5.x raises: 'LLaDAModelLM' object has no attribute 'all_tied_weights_keys')
#   - diffusers (SD 2.1 autoencoder) needs >=4.48 for Dinov2WithRegistersConfig
#   4.48–4.5x satisfies BOTH. Do NOT bump to 5.x without re-testing LLaDA load.
transformers>=4.48,<5.0
accelerate>=0.27.0            # required by diffusers, and by low_cpu_mem_usage LLaDA load
# diffusers: SDXL support is stable from 0.24+; callback_on_step_end (used by
# the denoising-trajectory instrumentation) needs >=0.27. SDXL loads the
# StableDiffusionXLPipeline + dual text encoders from this package.
diffusers>=0.27.0            # T2I backbones (Stable Diffusion 2.1 AND SDXL)
safetensors>=0.4.2           # fast weight loading; SDXL ships safetensors-only

# CLIP / Long-CLIP
open-clip-torch>=2.24.0       # optional: used if you swap to open_clip encoders

# Hugging Face Hub (model downloads)
huggingface-hub>=0.21.0

# LLaDA dependencies
numpy>=1.24.0

# Ollama AR rewriter
requests>=2.31.0

# Image processing
Pillow>=10.0.0

# SDXL note
# ---------
# SDXL base (stabilityai/stable-diffusion-xl-base-1.0) needs no extra packages
# beyond diffusers + transformers + safetensors above, but at 1024x1024 it is
# ~7 GB of weights + dual encoders. invisible-watermark is OPTIONAL — diffusers
# only imports it if watermarking is enabled (it is not, in this pipeline).
# If a future diffusers version warns about it, uncomment:
# invisible-watermark>=0.2.0

# Evaluation
scipy>=1.11.0                 # FID computation
torchmetrics>=1.3.0          # CLIPScore, FID wrappers
lpips>=0.1.4                 # perceptual similarity (optional)
torch-fidelity>=0.3.0        # FID backend

# Plotting (evaluation/plotting.py — RQ1-4 figures + trajectories)
matplotlib>=3.7.0

# NLP parsing (semantic density / scene-graph analysis in RQ1)
spacy>=3.7.0

# Experiment tracking
wandb>=0.16.0

# Config
PyYAML>=6.0.1

# Utilities
tqdm>=4.66.0
einops>=0.7.0                 # tensor rearrangement (used by some eval modules)

# Development
pytest>=8.0.0
